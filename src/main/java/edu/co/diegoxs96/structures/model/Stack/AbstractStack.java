package edu.co.diegoxs96.structures.model.Stack;

import edu.co.diegoxs96.structures.model.collection.AbstractCollection;

/**
 * Abstract base class for Stack implementations.
 * Extends AbstractCollection and implements Stack interface.
 *
 * @param <E> the type of elements in the stack.
 *
 * @author julian benitorevollo bernal
 * @version 1.0.20240219
 */
public abstract class AbstractStack<E> extends AbstractCollection<E> implements Stack<E> {
    // forEach is inherited from AbstractCollection
}
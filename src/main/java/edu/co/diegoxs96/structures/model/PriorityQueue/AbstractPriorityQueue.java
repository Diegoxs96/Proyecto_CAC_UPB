package edu.co.diegoxs96.structures.model.PriorityQueue;

import edu.co.diegoxs96.structures.model.collection.AbstractCollection;

/**
 * Abstract base class for priority queue implementations.
 *
 * @param <E> the type of elements in the priority queue.
 */
public abstract class AbstractPriorityQueue<E>
        extends AbstractCollection<E>
        implements PriorityQueue<E> {
    // forEach is inherited from AbstractCollection
}
